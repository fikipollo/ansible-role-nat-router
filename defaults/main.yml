---
# Determines which network interface faces the public network (internet, out)
nat_router_public_interface: eth1
# Determines which network interface faces the private network (internal network)
nat_router_private_interface: eth0
# Determines if we want to configure NAT using firewalld (default) or iptables
nat_router_mode: firewalld
